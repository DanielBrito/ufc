#include<stdio.h>
#include<math.h>

int main()
{
    float base, potencia;

    scanf("%f%f", &base, &potencia);

    //if(base>0 && potencia>0)
    //{
        printf("\n%.1f\n\n", pow(base,potencia));
    //}

    return 0;
}
