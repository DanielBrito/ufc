
/*Função que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem DECRESCENTE utilizando o algoritmo Bublle Sort*/
void bubbleSort(int n, int* v);

/*Função que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem DECRESCENTE utilizando o algoritmo Insertion Sort*/
void insertionSort(int n, int* v);

/*Função que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem DECRESCENTE utilizando o algoritmo Selection Sort*/
void selectionSort(int n, int* v);

/*Função RECURSIVA que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem CRESCENTE utilizando o algoritmo Bublle Sort*/
void bubbleSortRec(int n, int* v);

/*Função RECURSIVA que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem CRESCENTE utilizando o algoritmo Insertion Sort*/
void insertionSortRec(int n, int* v);

/*Função RECURSIVA que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem CRESCENTE utilizando o algoritmo Selection Sort*/
void selectionSortRec(int n, int* v);

/*Função que recebe como entrada um vetor de inteiros (v) de n posições e ordena os dados do vetor em ordem DECRESCENTE utilizando o algoritmo MergeSort*/
void mergeSort(int n, int* v);
void intercala(int p, int q, int r, int* v);